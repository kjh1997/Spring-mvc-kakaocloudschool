server:
  port: 8080
spring:
  profiles:
    active: local
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/spring-study-olle?characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: whdgns1002@
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create # update create none
    properties:
      hibernate:

    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
